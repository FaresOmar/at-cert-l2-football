export interface TeamStanding {
  teamId: number;
  rank: number;
  name: string;
  played: number;
  win: number;
  lose: number;
  draw: number;
  logo: string;
  goalsDiff: number;
  points: number;
}
